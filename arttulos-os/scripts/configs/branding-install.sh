#!/bin/bash
# Install ArttulOS branding (logos, wallpapers, themes)
set -e

echo "Installing branding..."
# Example: copy branding assets (update with real paths as needed)
cp -r ../branding/arttulos-logo.svg /mnt/sysimage/usr/share/pixmaps/
# Add wallpaper and theme installation as needed
